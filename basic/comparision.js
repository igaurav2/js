// same as before
console.log( 1 != 2)

// while comparision make sure that your datatype is same

console.log(null > 0)
console.log(null == 0)
console.log(null >= 0)  // ----- true in this case

// equality check(==) and comarisons( >=, <=, >, <) work differently
// comparisons convert null to number, treating it as 0;

console.log(undefined >=0) // undefined ke case me false